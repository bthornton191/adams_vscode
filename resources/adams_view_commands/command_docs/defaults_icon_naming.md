# defaults icon_naming

The defaults icon_naming command is used to set the default display style for icon names in the Adams View model.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `show_as` | String | Specifies how icon names are displayed. Options include showing the full name or a shortened form. |
