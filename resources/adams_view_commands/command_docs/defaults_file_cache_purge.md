# defaults file_cache_purge

The defaults file_cache_purge command is used to set the default behavior for purging the file cache.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `delete_on_exit` | Boolean | Specifies whether cached files are automatically deleted when Adams View exits. |
