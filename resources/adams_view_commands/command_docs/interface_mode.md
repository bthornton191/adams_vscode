# interface mode

Allows you to create Adams entities interactively in a repeated fashion.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `mode` | Existing Adams entity type | Specifies the entity type to draw/create |
| `repeat` | Existing options | User can select any given option, |
