# xy_plots template auto_zoom

Automatically fits the axes of a plot with respect to their referencing curves. Removes any previously specified axis limits.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `plot_name` | Existing Plot | Specifies a string of characters that identifies one or more existing plots. |
