# xy_plots fft_window delete

This command will delete an existing curve and remove it from an XY plot. Once deleted, the curve may not be retrieved.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `window_name` | An Existing Fft_window | Specifies an existing fft_window |
