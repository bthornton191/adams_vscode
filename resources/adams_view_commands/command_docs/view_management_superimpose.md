# view management superimpose

Toggles the superimpose mode for a view, allowing multiple model configurations to be displayed simultaneously.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `on_off` | Boolean | Whether to enable (`on`) or disable (`off`) superimpose mode. |
| `view_name` | Object | Name of the view to configure. |
