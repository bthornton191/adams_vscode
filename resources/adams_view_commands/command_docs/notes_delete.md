# notes delete

Allows you to delete a note. A note is a single text string, or a series of text strings used to add an annotation to an existing view or plot.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `note_name` | Existing Note | Specifies an existing note to be deleted. |
