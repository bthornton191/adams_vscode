# interface object_table execute

Allows you to execute the command associated with the object_table.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `object_table _name` | An Existing GI_otable | Specifies the name of the object_table for which the command is to be executed |
