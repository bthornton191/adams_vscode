# optimize objective evaluate

Allows you to evaluate one or more optimization objectives against a specific analysis result.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `objective_name` | An Existing Optimization Objective | Specifies the name or names of the optimization objectives to evaluate. |
| `analysis_name` | An Existing Analysis | Specifies the analysis result set used to evaluate the objectives. |
