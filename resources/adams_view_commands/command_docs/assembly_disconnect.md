# assembly disconnect

Allows you to disconnect one assembly instance from another.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `instance_name` | An Existing Assembly Instance | Specifies the assembly instance to disconnect. |
| `from_instance_name` | An Existing Assembly Instance | Specifies the assembly instance to disconnect from. |
