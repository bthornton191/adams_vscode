# panel set acf_twindow linear_solver

Configures the linear solver settings for an ACF test window panel.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `solver_type` | String | Type of linear solver to use. |
| `stability` | String | Stability parameter for the linear solver. |
