# defaults interface

The defaults interface command is used to set the default Adams View user interface style.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `style` | String | Specifies the default interface style (e.g., classic or ribbon). |
