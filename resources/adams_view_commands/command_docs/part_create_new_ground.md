# part create new_ground

Allows you to create a new part and makes it the ground part. This feature is useful when incrementally designing a model.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `part_name` | A New Part | Specifies the name of the new part. You may use this name later to refer to this part. |
