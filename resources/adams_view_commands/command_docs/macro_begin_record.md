# macro begin_record

Begins recording Adams View commands to a macro. All subsequent commands are captured until macro end_record is called.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `macro_name` | String | Specifies the name assigned to the macro being recorded. |
