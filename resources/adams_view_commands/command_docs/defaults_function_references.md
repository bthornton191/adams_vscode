# defaults function_references

The defaults function_references command is used to set the default display style for function references in the Adams View interface.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `show_as` | String | Specifies how function references are displayed. Options include showing the full expression or a simplified representation. |
