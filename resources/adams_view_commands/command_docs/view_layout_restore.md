# view layout restore

The RESTORE command allows you to replace the currently displayed views with the views in the specified layout.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `view_layout_name` | Existing View layout | Specifies an existing view layout. |
