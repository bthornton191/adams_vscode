# file postprocessing

Allows you to export data from Adams View for use with a stand-alone version of Adams PostProcessor. When you export Adams PostProcessor files, Adams View generates a command file (.cmd) and all required supporting files, including:

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `model_names` | Existing model | Specifies the models for which you want to export information. |
