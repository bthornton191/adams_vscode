# simulation single_run equilibrium

When you perform a static equilibrium simulation on your model, Adams Solver iteratively repositions all parts in an attempt to balance all the forces for one particular point in time.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `model_name` | Existing Model | Specifies the model name on which static equilibrium is to be performed. |
