# assembly replace instance

Allows you to replace an assembly instance with a different assembly type.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `instance_name` | An Existing Assembly Instance | Specifies the assembly instance to replace. |
| `with_type` | String | Specifies the assembly definition type to replace the instance with. |
