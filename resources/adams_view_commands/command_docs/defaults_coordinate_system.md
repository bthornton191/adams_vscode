# defaults coordinate_system

Allows you to select the coordinate system that location coordinates and orientation angles are with respect to by default.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `default_coordinate_system` | An Existing Model, Part Or Marker | Specifies the coordinate system that location coordinates and orientation angles are with respect to by default. |
