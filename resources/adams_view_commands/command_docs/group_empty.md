# group empty

Removes all objects from the group. Group continues to exist unlike the use of “group delete” command that deletes the group as well.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `group_name` | Existing Group | Specifies the group to empty. You use this parameter to identify the existing group to be affected with this command. |
