# defaults container

The defaults container command is used to set the default container used for newly created objects.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `container_name` | An Existing Container | Specifies the container to use as the default for newly created objects. |
