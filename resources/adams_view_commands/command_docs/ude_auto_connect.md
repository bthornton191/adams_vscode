# ude auto_connect

Automatically connects User-Defined Elements (UDEs) to model topology in the specified order.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `order` | Integer | Connection order; determines the sequence in which UDEs are auto-connected. |
