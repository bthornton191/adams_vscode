# assembly sync

Allows you to synchronize an assembly instance with its definition or with another source.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `instance_name` | An Existing Assembly Instance | Specifies the assembly instance to synchronize. |
| `from` | String | Specifies the source to synchronize from (for example, the definition or an external file). |
