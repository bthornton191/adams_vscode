# command_server start

Starts a new TCP/IP command server in Adams View that listens for commands and queries from external applications. The server listens on the port specified by the Adams View configuration (default: 5002).
