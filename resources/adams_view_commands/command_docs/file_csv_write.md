# file csv write

Allows you to write result set data to a comma-separated values (CSV) file.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `file_name` | String | Specifies the name of the CSV file to write. |
| `result_set_component` | An Existing Result Set Component | Specifies the result set component whose data is written to the file. |
