# interface cursor warp

Allows you to warp (reposition) the mouse cursor to a specific GUI element.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `gui_name` | String | Specifies the name of the GUI element to which the cursor is warped. |
