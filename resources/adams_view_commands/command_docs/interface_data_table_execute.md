# interface data_table execute

Allows you to execute a command specified while creating a data table.

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `data_table _name` | An Existing GI_data_table | Specifies the name of an existing data table for the execution of a command. |
